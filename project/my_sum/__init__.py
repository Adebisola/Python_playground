#!/usr/bin/python3
# defines a function that adds a list/tuple

def sum(arg):
    # represents a function that add the values in a list/tuple

    total = 0
    for val in arg:
        total += val
    return total
